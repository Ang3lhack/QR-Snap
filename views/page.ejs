<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .page-container { min-height: 100vh; display: flex; flex-direction: column; justify-content: center; }
        .content-box { max-width: 600px; margin: 0 auto; border-radius: 15px; }
        img { max-height: 400px; object-fit: cover; width: 100%; border-radius: 10px; }
    </style>
</head>
<body class="<%= page.theme %> page-container p-3">
    
    <div class="content-box w-100 p-4 shadow-lg" style="background-color: rgba(255, 255, 255, 0.1); backdrop-filter: blur(10px); border: 1px solid rgba(255,255,255,0.2);">
        
        <h1 class="display-5 fw-bold mb-2"><%= page.title %></h1>
        <p class="opacity-75 fst-italic mb-4">Por: <%= page.author %></p>
        
        <% if (page.image) { %>
            <div class="mb-4">
                <img src="<%= page.image %>" alt="Imagen del usuario" class="shadow-sm">
            </div>
        <% } %>

        <div class="message-content fs-5 mb-5" style="white-space: pre-wrap;"><%= page.message %></div>

        <div class="text-center mt-4 pt-3 border-top border-light opacity-50">
            <small>Vistas: <%= page.views %> | Creado con <strong>QR-Snap</strong></small>
            <br>
            <a href="/" class="text-reset text-decoration-none"><small>Crear mi propia página</small></a>
        </div>
    </div>

</body>
</html>